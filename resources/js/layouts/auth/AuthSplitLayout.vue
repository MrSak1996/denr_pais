<script setup lang="ts">
import AppLogoIcon from '@/components/AppLogoIcon.vue';
import { Link, usePage } from '@inertiajs/vue3';
const page = usePage();
const name = page.props.name;
const quote = page.props.quote;

defineProps<{
    title?: string;
    description?: string;
}>();
</script>
<style>
.login-wall {
    background-image: url('../../../images/bg8.jpg');
    background-size: cover;
    background-position: center;
}
</style>
<template>
    <div
        class="relative  grid h-dvh flex-col items-center justify-center px-8 sm:px-0 lg:max-w-none lg:grid-cols-2 lg:px-0">
        <div class="relative hidden h-full flex-col bg-muted p-10 text-red-950 lg:flex dark:border-r login-wall">
            <div class="absolute inset-0" />
            <Link :href="route('home')" class="relative z-20 flex items-center text-lg font-medium">


            </Link>
            <div v-if="quote" class="relative z-20 mt-auto  z-20 mt-auto">
                <blockquote class="space-y-2">
                    <p class="text-lg">&ldquo;{{ quote.message }}&rdquo;</p>
                    <footer class="text-sm text-neutral-300">{{ quote.author }}</footer>
                </blockquote>
            </div>
        </div>
        <div class="flex items-center justify-center px-6">
            <div class="w-full max-w-md rounded-2xl bg-white/90 backdrop-blur p-8 space-y-6">
                <div class="lg:p-8">
                    <div class="mx-auto flex w-full flex-col justify-center space-y-6 sm:w-[350px] ">
                        <div class="flex flex-col space-y-2 text-center">
                            <h1 class="text-xl font-medium tracking-tight" v-if="title">{{ title }}</h1>
                            <p class="text-sm text-muted-foreground" v-if="description">{{ description }}</p>
                        </div>
                        <slot />
                    </div>
                </div>
            </div>
        </div>
    </div>
</template>
